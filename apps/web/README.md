Session 5. Scaffold with `pnpm create next-app@latest web --ts --app --tailwind --no-eslint --src-dir --import-alias "@/*"` from `apps/`, then delete the boilerplate. Screens per pounce-mockups.html.
